'use strict';
module.exports = {
    // exec: require('./exec'),
    description: `
        My goal is to collect the phrase counts in a data structure that natively handles storing subphrases beneath their superphrases to avoid the awkward subphrase deletion step of solution2.

        This would require traversing the data structure at the end to get subphrases for non-repeated phrases.

    `,
    pseudo: `

    `,
};
